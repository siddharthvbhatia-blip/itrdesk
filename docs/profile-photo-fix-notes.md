# Profile photo resilience

This branch replaces the unsupported large inline image source with normal image assets, a PNG fallback, an independent-host fallback, runtime recovery and live decode tests.